'use strict';

const exec = require('child_process').exec;

module.exports = function (gulp) {
  // exec('ls', err => {
  //   if (err) return cb(err); // return error
  //   cb(); // finished task
  // });
};
